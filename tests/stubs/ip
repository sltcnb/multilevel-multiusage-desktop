#!/bin/sh
: "${STUB_LOG:=/tmp/stub.log}"
printf 'ip %s\n' "$*" >> "$STUB_LOG"
case "$*" in
  "route show default"*) echo "default via 192.168.1.1 dev ${STUB_WAN:-wlan0} proto dhcp metric 600" ;;
  "-o link show"*)       echo "1: lo: <LOOPBACK,UP>" ;;
esac
exit 0
